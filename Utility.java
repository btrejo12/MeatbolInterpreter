package meatbol;

public class Utility {
    public ResultValue subtract(Parser parser, ResultValue rv1, ResultValue rv2){
       ResultValue result = new ResultValue();

       return result;
    }
    public ResultValue add(Parser parser, ResultValue rv1, ResultValue rv2) {
        ResultValue result = new ResultValue();

        return result;
    }
    public ResultValue multiply(Parser parser, ResultValue rv1, ResultValue rv2) {
        ResultValue result = new ResultValue();

        return result;
    }
    public ResultValue divide(Parser parser, ResultValue rv1, ResultValue rv2) {
        ResultValue result = new ResultValue();

        return result;
    }
}
